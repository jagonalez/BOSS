import type { BackendId } from '@shared/backend'

export const BACKEND_SHORT_LABELS: Record<BackendId, string> = {
  opencode: 'OpenCode',
  pi: 'Pi',
  codex: 'Codex',
  claude: 'Claude',
  lab: 'Lab'
}
